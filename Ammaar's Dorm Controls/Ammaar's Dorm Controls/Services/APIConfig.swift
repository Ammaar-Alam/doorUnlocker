import Foundation

struct APIConfig {
    // Set your server base URL here
    static let baseURL = "https://door.ammaar.xyz"
    // If on a device and not the same network, ensure reachable endpoint or use a public URL
    // If using HTTPS or deployed on Heroku, update accordingly.
}
